# Naosite

Minimal static site scaffold created by Copilot.

Location: `apps/naosite`

Quick start:

1. Serve locally with `npx serve` or your preferred static server:

```bash
cd apps/naosite
npx serve -p 5173
```

2. Edit `index.html` and `styles.css`.

3. To add a build tool (Vite, Eleventy, etc.), let me know and I can scaffold that.
